[defaults]
inventory = inventory.yml
host_key_checking = False
timeout = 30
# WSL에서 Windows 파일시스템 world-writable 경고 무시
allow_world_readable_tmpfiles = True
remote_user = jw
# Bitwarden SSH Agent 동시 연결 문제 방지 — 순차 처리
forks = 1
