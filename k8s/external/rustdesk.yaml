apiVersion: gateway.envoyproxy.io/v1alpha1
kind: Backend
metadata:
  name: rustdesk
  namespace: default
spec:
  endpoints:
    - ip:
        address: 192.168.1.196
        port: 21114
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: rustdesk
  namespace: default
spec:
  parentRefs:
    - name: internal-gateway
  hostnames:
    - "rustdesk.local.example.com"
  rules:
    - backendRefs:
        - group: gateway.envoyproxy.io
          kind: Backend
          name: rustdesk
