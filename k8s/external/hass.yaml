apiVersion: gateway.envoyproxy.io/v1alpha1
kind: Backend
metadata:
  name: hass
  namespace: default
spec:
  endpoints:
    - ip:
        address: 192.168.1.199
        port: 8123
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: hass
  namespace: default
spec:
  parentRefs:
    - name: internal-gateway
  hostnames:
    - "hass.local.example.com"
  rules:
    - backendRefs:
        - group: gateway.envoyproxy.io
          kind: Backend
          name: hass
