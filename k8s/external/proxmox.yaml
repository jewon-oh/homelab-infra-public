apiVersion: gateway.envoyproxy.io/v1alpha1
kind: Backend
metadata:
  name: proxmox
  namespace: default
spec:
  endpoints:
    - ip:
        address: 192.168.1.114
        port: 8006
  tls:
    insecureSkipVerify: true
    sni: 192.168.1.114
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: proxmox
  namespace: default
spec:
  parentRefs:
    - name: internal-gateway
  hostnames:
    - "proxmox-wtr-pro.local.example.com"
  rules:
    - backendRefs:
        - group: gateway.envoyproxy.io
          kind: Backend
          name: proxmox
