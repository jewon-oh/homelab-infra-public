apiVersion: gateway.envoyproxy.io/v1alpha1
kind: Backend
metadata:
  name: adguard
  namespace: default
spec:
  endpoints:
    - ip:
        address: 192.168.1.196
        port: 80
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: adguard
  namespace: default
spec:
  parentRefs:
    - name: internal-gateway
  hostnames:
    - "adguard.local.example.com"
  rules:
    - backendRefs:
        - group: gateway.envoyproxy.io
          kind: Backend
          name: adguard
