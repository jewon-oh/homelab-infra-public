# Longhorn 반복 백업 Job
# backup-critical 그룹에 속한 PV만 매일 새벽 4시(UTC) S3 백업
# 최근 7개 보관
apiVersion: longhorn.io/v1beta2
kind: RecurringJob
metadata:
  name: daily-backup
  namespace: longhorn-system
spec:
  cron: "0 4 * * *"
  task: backup
  retain: 7
  concurrency: 2
  groups:
    - backup-critical

